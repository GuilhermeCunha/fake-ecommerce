import { IsString, IsOptional } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class Patch{{schemaName}}DTO {
  @ApiProperty({
    required: false,
  })
  @IsString()
  @IsOptional()
  example?: string;
}
