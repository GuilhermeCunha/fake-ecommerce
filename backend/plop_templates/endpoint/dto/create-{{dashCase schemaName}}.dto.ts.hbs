import { IsString } from 'class-validator';
import { ApiProperty } from '@nestjs/swagger';

export class Create{{schemaName}}DTO {
  @ApiProperty({
    required: true,
  })
  @IsString()
  example: string;
}
